import React from 'react'

const SectionTitle = ({text}) => {
  return (
    <div>
        <h2 className='capitalize text-3xl font-bold border-b pb-5'>{text}</h2>
    </div>
  )
}

export default SectionTitle;